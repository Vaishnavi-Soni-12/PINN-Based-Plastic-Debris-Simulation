# outputs/animations

## Purpose

Stores saved MP4 animations of concentration field evolution across simulation time steps.

## Generated By

- `utils/save_outputs.py` via `save_animation(...)`

## Requirements

`ffmpeg` must be installed and available on PATH for MP4 export.

## Version Control

Do not commit generated animation files.
